# SFFNet 实施运行完全指南 - 从零开始

**目标**：一步一步地让你能够运行这个项目，理解每一步在做什么

---

## 📋 前置准备检查清单

在开始之前，确保你有：

- [ ] **Windows 10/11** 系统
- [ ] **Python 3.8** 已安装
- [ ] **NVIDIA GPU**（推荐）或者你有耐心等待 CPU 运行
- [ ] 至少 **20GB** 的硬盘空间
- [ ] **网络连接**（下载模型和数据）

---

## 🚀 第一阶段：环境配置（30分钟）

### 步骤 1.1：检查 Python 版本

打开 PowerShell，输入：

```powershell
python --version
```

**预期输出**：
```
Python 3.8.x
```

如果没有或版本不对，需要先安装 Python 3.8。

### 步骤 1.2：创建虚拟环境

虚拟环境的作用：隔离项目依赖，避免不同项目之间的冲突

```powershell
# 进入项目目录
cd e:\Graduation_Project\SFFNet

# 创建虚拟环境
python -m venv sffnet_env

# 激活虚拟环境
sffnet_env\Scripts\activate

# 成功激活后，命令行前面会显示 (sffnet_env)
```

### 步骤 1.3：或使用 Conda（如果已安装）

```powershell
# 创建 conda 环境
conda create -n sffnet python=3.8

# 激活环境
conda activate sffnet

# 验证激活
python --version  # 应该显示 3.8.x
```

### 步骤 1.4：安装 PyTorch

这一步很关键！PyTorch 是深度学习框架。

```powershell
# GPU 版本（推荐，需要 NVIDIA GPU）
pip install torch==1.12.1 torchvision==0.13.1 torchaudio==0.12.1 --index-url https://download.pytorch.org/whl/cu113

# 或者 CPU 版本（如果没有 GPU）
pip install torch==1.12.1 torchvision==0.13.1 torchaudio==0.12.1
```

**验证安装**：
```powershell
python -c "import torch; print(torch.__version__); print(torch.cuda.is_available())"
```

**预期输出**：
```
1.12.1
True        # 如果有 GPU，显示 True；CPU 显示 False
```

### 步骤 1.5：安装其他依赖

```powershell
# 进入项目目录（如果还没进去）
cd e:\Graduation_Project\SFFNet

# 安装 requirements.txt 中的所有依赖
pip install -r requirements.txt

# 特别地，小波分析库可能需要单独安装
pip install pytorch_wavelets
```

**这会安装以下库**：
- timm: 预训练模型
- pytorch-lightning: 训练框架
- albumentations: 数据增强
- einops: 张量操作
- scikit-image: 图像处理
- 等等...

**完整安装耗时**：5-15分钟（取决于网速）

### 步骤 1.6：验证安装完成

```powershell
# 运行 Python 脚本验证所有库都能导入
python -c "
import torch
import pytorch_lightning
import timm
import torchvision
import numpy as np
print('✓ 所有关键库已正确安装！')
"
```

如果没有错误，继续。如果有错误，查看错误信息并重新安装。

---

## 🎯 第二阶段：数据准备（30分钟-2小时）

### 步骤 2.1：理解数据目录结构

```
data/
├── vaihingen/
│   ├── train/
│   │   ├── images_1024/      # 训练图像（.tif 格式）
│   │   └── masks_1024/       # 训练标签（.tif 格式）
│   └── test/
│       ├── images_1024/      # 测试图像
│       └── masks_1024/       # 测试标签
```

### 步骤 2.2：获取数据集

**方法 A：使用预处理的数据集（推荐）**

如果导师已经提供了预处理后的数据：

1. 将数据放在 `e:\Graduation_Project\SFFNet\data\vaihingen\` 目录下

**方法 B：从 HuggingFace 下载**

README 中提到可以从 HuggingFace 下载预处理的 Vaihingen 数据集：

```powershell
# 首先安装 huggingface_hub
pip install huggingface_hub

# 下载数据集
# （这个命令可能很长，具体查看 README）
```

**验证数据集**：
```powershell
# 检查文件是否存在
Get-ChildItem -Recurse data/vaihingen/train/images_1024/ | Measure-Object | Select-Object -ExpandProperty Count
# 应该显示训练图像数量

Get-ChildItem -Recurse data/vaihingen/test/images_1024/ | Measure-Object | Select-Object -ExpandProperty Count
# 应该显示测试图像数量
```

### 步骤 2.3：快速查看数据

让我们看看数据长什么样：

```python
# 保存为 view_data.py
import numpy as np
from PIL import Image
import matplotlib.pyplot as plt

# 查看一张训练图像
img_path = 'data/vaihingen/train/images_1024/top_potsdam_2_10_RGB.tif'
label_path = 'data/vaihingen/train/masks_1024/top_potsdam_2_10_label.tif'

# 读取图像
img = Image.open(img_path)
label = Image.open(label_path)

print(f"图像大小: {img.size}")
print(f"标签大小: {label.size}")
print(f"标签类型: {label.mode}")

# 显示
fig, axes = plt.subplots(1, 2, figsize=(12, 5))
axes[0].imshow(img)
axes[0].set_title('RGB 图像')
axes[1].imshow(label, cmap='tab20')  # 使用分类颜色表
axes[1].set_title('语义标签')
plt.tight_layout()
plt.savefig('sample_data.png')
print("已保存样本数据可视化到 sample_data.png")
```

运行：
```powershell
python view_data.py
```

---

## ⚙️ 第三阶段：代码理解测试（30分钟）

### 步骤 3.1：测试数据加载

创建 `test_dataloader.py`：

```python
from GeoSeg.geoseg.datasets.vaihingen_dataset import VaihingenDataset
from torch.utils.data import DataLoader

# 创建数据集对象
dataset = VaihingenDataset(
    data_root='data/vaihingen/train',
    mode='train',
    mosaic_ratio=0.25,
    transform=None  # 暂不使用数据增强
)

print(f"数据集大小: {len(dataset)} 张图像")

# 查看单个样本
image, label = dataset[0]
print(f"单个图像形状: {image.shape}")  # 应该是 (3, 512, 512) 或类似
print(f"单个标签形状: {label.shape}")  # 应该是 (512, 512)
print(f"标签类别范围: {label.min()} 到 {label.max()}")

# 创建 DataLoader
dataloader = DataLoader(
    dataset,
    batch_size=2,
    num_workers=0,  # Windows 上建议用 0
    shuffle=True
)

# 获取一个 batch
for images, labels in dataloader:
    print(f"\nBatch 图像形状: {images.shape}")  # (2, 3, 512, 512)
    print(f"Batch 标签形状: {labels.shape}")    # (2, 512, 512)
    break
```

运行测试：
```powershell
python test_dataloader.py
```

**预期输出**：
```
数据集大小: 45 张图像
单个图像形状: (3, 512, 512)
单个标签形状: (512, 512)
标签类别范围: 0 到 5

Batch 图像形状: torch.Size([2, 3, 512, 512])
Batch 标签形状: torch.Size([2, 512, 512])
```

### 步骤 3.2：测试模型前向传播

创建 `test_model.py`：

```python
import torch
from GeoSeg.geoseg.models.SFFNet.SFFNet import SFFNet

# 创建模型
model = SFFNet(num_classes=6, pretrained=True)
print("✓ 模型加载成功")

# 确保模型在正确的设备上
device = torch.device('cuda' if torch.cuda.is_available() else 'cpu')
model.to(device)
print(f"✓ 模型已移到 {device}")

# 创建虚拟输入
batch_size = 2
dummy_input = torch.randn(batch_size, 3, 512, 512).to(device)
print(f"✓ 虚拟输入形状: {dummy_input.shape}")

# 前向传播
model.eval()  # 评估模式（不进行 dropout）
with torch.no_grad():
    output = model(dummy_input)
    
print(f"✓ 模型输出形状: {output.shape}")  # 应该是 [2, 6, 512, 512]
print(f"✓ 输出值范围: [{output.min():.4f}, {output.max():.4f}]")

print("\n✅ 模型测试成功！")
```

运行测试：
```powershell
python test_model.py
```

**预期输出**：
```
✓ 模型加载成功
✓ 模型已移到 cuda
✓ 虚拟输入形状: torch.Size([2, 3, 512, 512])
✓ 模型输出形状: torch.Size([2, 6, 512, 512])
✓ 输出值范围: [-0.8534, 2.1256]

✅ 模型测试成功！
```

---

## 🏋️ 第四阶段：单机迷你训练（15分钟）

在运行完整训练前，先用很少的数据测试一下训练循环。

### 步骤 4.1：修改配置文件进行测试

编辑 `GeoSeg/config/vaihingen/sffnet.py`，修改这些参数以加快测试：

```python
# 修改前：
max_epoch = 105
train_batch_size = 2

# 修改后：
max_epoch = 2              # 只训练2个 epoch（快速测试）
train_batch_size = 1       # 更小的 batch size
check_val_every_n_epoch = 1

# 其他保持不变
lr = 6e-4
num_classes = len(CLASSES)
```

### 步骤 4.2：运行迷你训练

```powershell
# 激活虚拟环境（如果还没激活）
sffnet_env\Scripts\activate

# 运行训练
python GeoSeg/train_supervision.py -c GeoSeg/config/vaihingen/sffnet.py
```

**这会发生什么**：
1. 模型初始化（几秒）
2. 第一个 epoch 开始（会打印进度条）
3. 输出类似：
```
Epoch 0/1
  [████████████████] 23/23 [00:45<00:00, 2.19s/batch]
  Train Loss: 0.45
  Val Loss: 0.52
  Val mIoU: 0.12

Epoch 1/1
  [████████████████] 23/23 [00:44<00:00, 1.92s/batch]
  Train Loss: 0.38
  Val Loss: 0.47
  Val mIoU: 0.18

Training complete!
```

**日志文件位置**：`lightning_logs/vaihingen/sffnet-convnext-512-crop-ms-e105/`

**如果看到 NaN loss 或错误**：
- 检查数据路径是否正确
- 检查batch_size是否设置为在显存中
- 查看具体错误消息

---

## 🎓 第五阶段：完整训练（4-8小时）

### 步骤 5.1：恢复完整配置

编辑 `GeoSeg/config/vaihingen/sffnet.py`，恢复原始配置：

```python
max_epoch = 105            # 恢复为完整训练
train_batch_size = 2       # 恢复为原始 batch size
check_val_every_n_epoch = 1
```

### 步骤 5.2：启动完整训练

```powershell
# 如果想在后台运行（推荐），使用 nohup 或 screen
# 但在 Windows 上，推荐直接运行，或使用 task scheduler

python GeoSeg/train_supervision.py -c GeoSeg/config/vaihingen/sffnet.py

# 或者如果想让这个进程在后台继续运行
# 可以在另一个 PowerShell 窗口中查看日志
```

### 步骤 5.3：监控训练过程

**打开 TensorBoard 查看实时曲线**：

在新的 PowerShell 窗口中：

```powershell
cd e:\Graduation_Project\SFFNet
tensorboard --logdir=lightning_logs/vaihingen
```

然后在浏览器中打开：`http://localhost:6006`

**你能看到**：
- 损失函数曲线
- mIoU曲线
- 学习率变化

**监控的关键指标**：

| 指标 | 好的趋势 | 需要действий |
|------|---------|-----------|
| **Train Loss** | 持续下降 ↘️ | 如果增加 ↑ 或不动，学习率可能太高 |
| **Val Loss** | 逐渐下降 ↘️ | 如果增加，可能过拟合 |
| **Val mIoU** | 持续增加 ↗️ | 如果停滞不前，可能已收敛 |

---

## 🧪 第六阶段：评估和可视化（30分钟）

训练完成后（模型保存在 `model_weights/vaihingen/`），进行评估。

### 步骤 6.1：运行评估

```powershell
python GeoSeg/vaihingen_test.py \
    -c GeoSeg/config/vaihingen/sffnet.py \
    -o fig_results/vaihingen \
    -t \
    --rgb
```

**参数说明**：
- `-c`: 配置文件路径
- `-o`: 输出结果目录
- `-t`: 启用测试时增强（TTA）
- `--rgb`: 输出彩色标签（易于查看）

### 步骤 6.2：查看结果

结果保存在 `fig_results/vaihingen/`：

```powershell
# 列出所有预测结果
Get-ChildItem fig_results/vaihingen/ | head -20
```

**会生成**：
- `*_pred.png`: 彩色预测结果
- `*_mask.png`: 真实标签
- 评估指标（在控制台输出）

### 步骤 6.3：可视化对比

创建 `visualize_results.py`：

```python
import matplotlib.pyplot as plt
from PIL import Image
import os

# 选择一个预测结果
test_idx = 0
img_name = sorted(os.listdir('fig_results/vaihingen/'))[test_idx]

pred_path = f'fig_results/vaihingen/{img_name}'
orig_path = f'data/vaihingen/test/images_1024/{img_name.replace("_pred", "")}'

# 读取图像
orig = Image.open(orig_path)
pred = Image.open(pred_path)

# 显示
fig, axes = plt.subplots(1, 3, figsize=(15, 5))
axes[0].imshow(orig)
axes[0].set_title('原始图像')
axes[1].imshow(pred)
axes[1].set_title('模型预测')
axes[2].text(0.1, 0.5, '模型已成功进行了分割预测！', fontsize=16)
axes[2].axis('off')
plt.tight_layout()
plt.savefig('results_comparison.png', dpi=150)
print("✓ 已保存对比图到 results_comparison.png")
```

运行看看效果：
```powershell
python visualize_results.py
```

---

## 📊 第七阶段：理解输出指标

评估脚本会输出：

```
Model Performance on Vaihingen Test Set
=====================================
Overall Accuracy: 0.8523
Mean Accuracy: 0.7245
Mean IoU: 0.6782

Per-class IoU:
  Class 0 (背景/杂物): 0.7234
  Class 1 (建筑):     0.8234
  Class 2 (道路):     0.6123
  Class 3 (树木):     0.5432
  Class 4 (低植被):   0.4234
  Class 5 (水):       0.8934
```

**指标解释**：

1. **Overall Accuracy** (客观正确率) = 正确像素数 / 总像素数
2. **Mean Accuracy** (平均正确率) = 各类别正确率的平均值
3. **Mean IoU** (平均交并比) = **最重要！** 各类别 IoU 的平均值
   - 范围 [0, 1]，越接近1越好
   - > 0.5：还可以
   - > 0.7：很好
   - > 0.85：优秀

---

## 🔧 常见问题调试

### 问题 1：显存不足 (CUDA out of memory)

```
RuntimeError: CUDA out of memory. Tried to allocate 2.00 GiB on cuda:0
```

**解决**：
1. 减小 batch_size：`train_batch_size = 1`
2. 减小输入分辨率：512 → 256
3. 减少梯度累积

### 问题 2：训练很慢

**可能原因**：
- 使用了 CPU 而不是 GPU
  ```powershell
  # 检查
  python -c "import torch; print(torch.cuda.is_available())"
  ```
- num_workers 设置过大：改为 0（Windows）或 2（Linux）

### 问题 3：损失函数为 NaN

```
Loss is NaN!
```

**解决**：
1. 检查数据是否正确加载（没有 NaN 值）
2. 降低学习率 `lr = 6e-4 → 1e-5`
3. 检查数据归一化是否正确

### 问题 4：迭代速度不稳定

**可能原因**：
- 输入数据大小不统一
- num_workers 过多导致内存竞争

**解决**：
- 检查数据预处理
- 减少 num_workers

---

## 📚 测试完整检查清单

```
□ 环境安装完成
  □ Python 3.8
  □ PyTorch 能导入
  □ GPU/CUDA 可用
  
□ 数据准备完成
  □ 训练数据放在正确位置
  □ 测试数据放在正确位置
  □ 能用脚本加载单个样本
  
□ 代码测试完成
  □ DataLoader 能输出正确的数据形状
  □ 模型能接收输入并输出预测
  □ 损失函数能计算
  
□ 迷你训练完成
  □ 训练循环跑通
  □ 损失函数在下降
  □ 评估指标在改进
  
□ 完整训练完成
  □ 105 个 epoch 全部训练完成
  □ 模型权重已保存
  
□ 评估完成
  □ 在测试集上评估
  □ mIoU 值在合理范围（> 0.65）
  □ 生成了可视化结果
```

---

## 🎯 后续进阶探索

完成上述所有步骤后，你可以尝试：

1. **修改超参数**
   - 调整学习率
   - 改变 backbone（ResNet → ConvNeXt）
   - 调整数据增强比例

2. **使用其他数据集**
   - LoveDA 数据集
   - Potsdam 数据集
   - 自己的遥感图像

3. **理解模型细节**
   - 阅读论文原文
   - 深入研究小波变换
   - 修改 MDAF 模块进行实验

4. **性能优化**
   - 模型量化（降低精度，加快推理）
   - 知识蒸馏（训练较小的学生模型）
   - 混合精度训练（fp16）

---

**下一步**：确认环境配置完成，然后开始运行测试！
